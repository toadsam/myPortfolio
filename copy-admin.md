# 관리자 페이지 (`/admin`) — Daily Village Journal

> 오늘의 기록이 마을을 바꿉니다
>
> 운동, 공부, 코딩, 프로젝트 작업, GitHub 활동을 기록하면 포트폴리오 마을의 조명과 NPC 상태가 자동으로 달라집니다.

- **경로:** `http://localhost:3000/admin` (`src/app/admin/page.tsx`)
- **성격:** 방문자용이 아닌 **본인 전용 기록 도구**. 여기 입력한 하루치 활동이 3D 마을의 건물 조명·NPC 무드·장식 해금으로 변환됩니다.
- **연결 대상:** FastAPI 백엔드 (`NEXT_PUBLIC_API_BASE_URL`, 기본 `http://localhost:8000`)
- **한 줄 요약:** 관리자 입력(활동 로그) → `village_service.derive_village_state` → 마을 상태 + NPC 컨텍스트

---

## 00. 접근 · 인증 (Admin Gate)

| | |
|---|---|
| 화면 | `Admin` / **관리자 인증** / "이 페이지는 정재훈 본인만 기록을 남길 수 있어요. 비밀번호를 입력해 주세요." |
| 입력 | 비밀번호 1개 (`type="password"`, autoFocus) |
| 버튼 | **입장** (진행 중엔 "확인 중…") · `← 마을로 돌아가기` |
| 실패 문구 | "비밀번호가 올바르지 않습니다." |

동작 순서

1. 진입 시 `GET /admin/auth-status`로 인증 사용 여부(`auth_enabled`) 확인
2. 인증이 켜져 있고 토큰이 없으면 → 잠금 화면(`AdminGate`)
3. `POST /admin/login`으로 토큰 발급 → 잠금 해제 후 전체 데이터 로드
4. **백엔드가 응답하지 않으면 잠금 없이 통과** — 로컬 개발 편의를 위한 의도된 동작이며, 실제 실패는 저장 시점에 메시지로 드러납니다
5. 인증 확인 전 화면: "불러오는 중…"

---

## 01. 화면 구조

```
헤더 (타이틀 · 마을 보기 · 오늘 기록 저장)
날짜 네비게이션 (◀ / date / ▶ · 오늘로 · 어제 기록 복사 · 기록 유무 배지)
┌ 본문(좌) ─────────────────────┬ 사이드바(우, 410px) ─┐
│ 오늘 컨디션                    │ 기록 스트릭            │
│ 운동 기록 · 공부 기록 (2열)    │ 데일리 목표(링 3개)    │
│ 코딩 기록                      │ 오늘 기록 요약         │
│ 프로젝트별 작업 시간           │ 활성 프로젝트          │
│                                │ 현재 마을 상태         │
│                                │ 상태 메시지 + 저장 버튼│
└────────────────────────────────┴────────────────────────┘
코딩테스트 풀이 기록 (Algorithm Dojo)
CS 전공지식 노트 (Knowledge Archive)
저장 후 → 리워드 모달 "오늘 마을에 생긴 일 ✨"
```

### 날짜 네비게이션

- `◀ / ▶`로 하루씩 이동, 미래 날짜는 선택 불가(`max=today`, ▶ 비활성)
- **오늘로** — 오늘 날짜로 복귀 (오늘이 아닐 때만 표시)
- **어제 기록 복사** — 전날 기록을 폼에 그대로 불러옴 (없으면 "어제 기록이 없어 복사할 내용이 없습니다.")
- 배지: `오늘 · 기록 있음` / `7월 27일 (일) · 빈 날`

---

## 02. 입력 패널

### 오늘 컨디션 (Daily Check-in)

| 필드 | 형태 | 값 |
|------|------|-----|
| 오늘 기분 | select | 차분함(steady) · 집중됨(focused) · 피곤함(tired) · 뿌듯함(proud) |
| 집중도 | 슬라이더 0~100 (기본 50) | 좌: 느슨함 / 우: 몰입 |
| 오늘 한 줄 메모 | text | 예: "FestFlow SSE 흐름을 정리하고 운동까지 완료" |

### 운동 기록 (Body Energy)

| 필드 | 형태 | 비고 |
|------|------|------|
| 오늘 운동 완료 | 체크박스 | "운동 기록은 중앙 광장과 가이드 NPC 상태에 반영됩니다." |
| 운동 시간 | number(분) | 운동 완료 체크 전에는 비활성 |
| 운동 종류 | select | 헬스 · 러닝 · 산책 · 스트레칭 · 등산 · 기타 |

### 공부 기록 (Study Energy)

| 필드 | 형태 | 비고 |
|------|------|------|
| 공부 시간 | number(분) | |
| 공부한 주제/기술 | 태그 입력 + 태그 클라우드 | Enter 또는 **추가**로 등록 |

빠른 선택 태그: `알고리즘` `React` `Next.js` `FastAPI` `Spring Boot` `Three.js` `Unity` `DB 설계`
※ 태그를 켜면 `studied_tech`(선택됨) + `study_topics`(누적 목록) 양쪽에 반영됩니다.

### 코딩 기록 (Development Log)

| 필드 | 형태 | 비고 |
|------|------|------|
| 총 코딩 시간 | number(분) | |
| GitHub 커밋 | number | 수동 입력 가능 |
| GitHub 커밋 동기화 | 버튼 | `POST /github/sync` → 오늘 커밋 수 자동 반영 |
| 오늘 건드린 GitHub repo | 칩 입력 | 예: `toadsam/FestFlow` |

### 프로젝트별 작업 시간 (Project Work)

- `src/data/projects.ts`의 전 프로젝트를 카드로 나열 (제목 + 설명 2줄 + `{분}m` 배지)
- `-30` / 직접 입력 / `+30` 버튼으로 분 단위 조정, 0분 초과 시 카드가 `active` 스타일
- 저장 시 0분 항목은 제거되어 전송(`normalizeForm`)

---

## 03. 사이드바 (실시간 피드백)

### 기록 스트릭 (Keep Going)

| 지표 | 계산 |
|------|------|
| 연속 기록 | 활동 점수 > 0 인 날의 연속 일수 (오늘이 비어 있으면 어제부터 셈) |
| 운동 연속 | `workout_done` 연속 일수 |
| 이번 주 코딩 | 최근 7일 코딩 분 합계 |

**오늘 AI 호출** — `GET /admin/overview`의 `ai_usage`로 `사용/한도` 게이지 표시 (60% 주황 / 90% 빨강). 조회 실패해도 기록 화면은 그대로 동작합니다.

**활동 캘린더** — 최근 16주(112일) 잔디. 셀 클릭 시 해당 날짜 편집으로 이동.

활동 점수(캘린더 색상 기준):

```
raw = 공부분/3 + 코딩분/3 + 커밋수×8 + (운동 여부 ? 20 : 0) + 프로젝트분/5
level = 0(raw 0) · 1(<20) · 2(<45) · 3(<80) · 4(80+)
```

### 데일리 목표 (Daily Rings)

| 링 | 목표 | 색 |
|----|------|----|
| 운동 | 30분 | 주황 `#f97316` |
| 코딩 | 120분 | 파랑 `#0284c7` |
| 공부 | 60분 | 초록 `#16a34a` |

- 미달성: "세 링을 다 채우면 하루 완성!"
- 전부 달성: "🎉 오늘 목표 세 개 다 채웠어요!"

### 오늘 기록 요약 (Village Impact)

운동 / 공부 / 코딩 / 프로젝트 합계 4칸 + **저장하면 바뀌는 것** 예고 리스트.

| 조건 | 예고 문구 |
|------|-----------|
| 운동 완료 + 시간 > 0 | 중앙 광장과 가이드 NPC가 {종류} {분}분 기록으로 활기차집니다. |
| 공부 > 0 | 기술 스택 구역이 공부 {분}분과 선택한 기술 주제에 따라 밝아집니다. |
| 코딩 > 0 | 코딩 {분}분이 개발자 NPC의 집중도와 마을 전체 활기에 반영됩니다. |
| 프로젝트 시간 입력 | 작업한 프로젝트 {n}개의 건물이 작업 시간에 따라 밝아집니다. |
| 커밋/repo 입력 | GitHub 커밋 {n}개와 repo 기록이 프로젝트 구역 에너지로 들어갑니다. |
| 메모 작성 | 오늘 메모가 경험 기록관과 NPC 상태 문구에 반영됩니다. |
| 아무것도 없음 | 아직 기록이 없어 마을이 조용한 상태로 유지됩니다. |

### 활성 프로젝트 / 현재 마을 상태

- **활성 프로젝트** — 시간 입력된 프로젝트 상위 5개. "이 프로젝트 건물 조명과 프로젝트 NPC 추천에 반영됩니다."
- **현재 마을 상태** — 백엔드가 계산한 `summary` + 활동 점수 상위 6개 건물의 `light_level`·사유

---

## 04. 저장 흐름과 마을 반영

```
[오늘 기록 저장]
  → POST /activity   (normalizeForm: 중복 태그 제거 · 0분 프로젝트 제거)
  → GET /activity/history?days=140  +  GET /village-state
  → 오늘 날짜였다면 리워드 모달 표시
```

상태 메시지

| 상황 | 문구 |
|------|------|
| 진입 | 오늘의 기록을 불러오는 중입니다. |
| 로드 완료 | 오늘의 기록을 불러왔습니다. |
| 저장 중 | 기록을 저장하고 마을 상태를 다시 계산하는 중입니다. |
| 저장 성공 | 기록이 저장되었습니다. 3D 마을 조명, NPC 상태, 장식이 갱신됐습니다. |
| 저장 실패 | 기록 저장에 실패했습니다. 백엔드 상태를 확인하세요. |
| 백엔드 미연결 | FastAPI 백엔드에 연결하지 못했습니다. 백엔드를 실행한 뒤 새로고침하세요. |
| GitHub 동기화 실패 | GitHub 동기화에 실패했습니다. 토큰 또는 백엔드 상태를 확인하세요. |

### 장식 해금 조건 (`village_service.py`)

| 해금 | 조건 |
|------|------|
| 🏋️ 운동 동상 (`training-statue`) | 운동 완료 |
| 🔦 연구소 등대 (`lab-beacon`) | GitHub 커밋 5개 이상 |
| ⛲ 학습 분수 (`study-fountain`) | 공부 120분 이상 |
| 💻 몰입 터미널 (`deep-work-terminal`) | 코딩 180분 이상 |
| `active-{projectId}` | 작업 시간이 입력된 프로젝트 상위 3개 |

### 리워드 모달 — "오늘 마을에 생긴 일 ✨"

`Village Updated` 라벨 아래 ① 마을 요약 ② **밝아진 건물** 칩(중앙 광장 제외 상위 6개) ③ **새로 해금** 칩 ④ **루미의 한마디**(가이드 NPC 상태 문구) ⑤ `마을에서 직접 보기 →` 링크.

---

## 05. 코딩테스트 풀이 기록 (Algorithm Dojo)

| 필드 | 형태 | 예시 |
|------|------|------|
| 날짜 | date | 선택한 날짜가 기본값 |
| 플랫폼 | select | 백준 · 프로그래머스 · 리트코드 · SWEA · 기타 |
| 문제 번호 (선택) | text | 1463 |
| 난이도 | text | 실버3, Lv.2 |
| 문제 제목 | text (필수) | 1로 만들기 |
| 언어 | select | Python · Java · C++ · JavaScript · C · Kotlin · 기타 |
| 문제 링크 (선택) | url | https://www.acmicpc.net/problem/1463 |
| 풀이 방법 / 접근 | textarea | 어떤 접근으로 풀었는지, 막혔던 부분, 시간복잡도 등 |
| 코드 | textarea (monospace) | 제출한 코드 붙여넣기 |

- 저장: `POST /admin/coding-tests` · 수정: `PUT /admin/coding-tests/{id}` · 삭제: `DELETE /admin/coding-tests/{id}` · 목록: `GET /coding-tests`
- 목록은 날짜별 그룹 + 제목·플랫폼·언어 검색, 항목 클릭 시 수정 모드(`#12 수정 중`)
- 제목 미입력 시: "문제 제목을 입력하세요."
- 저장 성공 문구: **"코딩테스트 풀이가 저장됐습니다. 알고리즘 도장이 밝아지고 알고가 이 풀이를 기억합니다."**

## 06. CS 전공지식 노트 (Knowledge Archive)

| 필드 | 형태 | 값 |
|------|------|-----|
| 날짜 | date | 선택한 날짜가 기본값 |
| 분야 | select | 운영체제 · 네트워크 · 데이터베이스 · 자료구조 · 알고리즘 · 컴퓨터구조 · 기타 |
| 제목 | text (필수) | 예: 프로세스와 스레드의 차이 |
| 공부한 내용 | textarea | 마크다운 없이 자유롭게 작성 가능 |

- 저장: `POST /admin/cs-notes` · 수정 `PUT` · 삭제 `DELETE` · 목록 `GET /cs-notes`
- 저장 성공 문구: **"CS 전공지식 노트가 저장됐습니다. 지식 서고가 밝아지고 노바가 이 내용을 기억합니다."**

---

## 07. 사용하는 API 한눈에

| 목적 | 엔드포인트 |
|------|------------|
| 인증 여부 | `GET /admin/auth-status` |
| 로그인 | `POST /admin/login` |
| 활동 저장 | `POST /activity` |
| 활동 이력(140일) | `GET /activity/history?days=140` |
| 마을 상태 | `GET /village-state` |
| AI 사용량 | `GET /admin/overview` |
| GitHub 동기화 | `POST /github/sync` |
| 코딩테스트 | `GET /coding-tests` · `POST·PUT·DELETE /admin/coding-tests` |
| CS 노트 | `GET /cs-notes` · `POST·PUT·DELETE /admin/cs-notes` |

---

## 08. 이 페이지의 설계 의도 (포트폴리오 서술용)

- **입력을 콘텐츠로 바꾸는 파이프라인** — 관리자 한 번의 저장이 건물 조명 점수, NPC 무드, 장식 해금, NPC 대화 컨텍스트까지 한 번에 갱신됩니다. "관리자 화면"이 아니라 **콘텐츠 생성기**로 설계했습니다.
- **저장 전에 결과를 보여준다** — '저장하면 바뀌는 것' 예고, 데일리 링, 잔디 캘린더로 입력의 결과를 즉시 되돌려 주어 기록을 계속하게 만드는 루프를 만들었습니다.
- **부가 정보 실패는 화면을 막지 않는다** — AI 사용량 조회가 실패해도 조용히 무시하고, 백엔드가 없으면 인증을 통과시키되 저장 시점에 명확한 메시지를 노출합니다. (핵심 기능과 부가 기능의 실패 처리를 분리)
- **데이터 지구 패턴** — 코딩테스트/CS 노트처럼 새 기록 종류를 추가하면 `모델 → 서비스 → 라우트 → 마을 조명 → 전담 NPC`가 같은 흐름으로 확장됩니다.
