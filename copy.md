# 정재훈 | Full-Stack (Web) · Unity XR Developer

> 웹을 주력으로, 운영 문제까지 해결하는 개발자 정재훈

- React · Spring Boot 기반 Full-Stack(Web) 개발
- 인증·보안(Refresh Token Rotation)과 운영 이슈(HTTPS·CORS) 해결 경험
- Unity XR·게임 개발 경험 보유

**요약:** 웹 주력 + 운영 이슈 해결 중심 · Main 13 / Sub 11 / Education 6

---

## 01. 기술 스택 (Skills)

`HTML5` `CSS3` `JavaScript` `React` `Node.js` `Spring Boot` `Database` `AWS` `Unity` `C#` `Game Dev` `3D`

| 영역 | 내용 |
|------|------|
| **Frontend** | React/TypeScript 기반 SPA 설계 및 구현 — 컴포넌트 구조화, 상태 흐름 설계, API 연동 |
| **Backend** | Spring Boot + JPA로 REST API 설계/구현 |
| **Auth** | JWT 인증/인가 + Refresh Token Rotation |
| **Infra** | AWS 배포 및 HTTPS / Mixed Content / CORS 해결 |
| **Unity XR/AR** | 인터랙션 및 상태/AI 제어 경험 |

**숙련도** *(임시 수치 — 조정 필요)*

- Web (Frontend): 85%
- Web (Backend): 80%
- Game / XR: 70%

---

## 02. 학력 · 경력 (Education)

### 아주대학교 — 디지털미디어학과 (전공)

`2021.03 ~ 2026.02 (예정)`
웹/소프트웨어 엔지니어링 중심으로 학습하며 서비스 구조 설계와 구현 역량을 확장했습니다.

- 웹(React/Spring Boot) 중심 프로젝트 경험
- Unity XR/AR 프로젝트로 인터랙션 경험 확장

### 아주대학교 — 인공지능 융합학과 (복수전공)

`2021.03 ~ 2026.02 (예정)`
AI/데이터 기반 개발 역량을 함께 확장하고 있습니다.

- 웹 개발과의 융합 관점으로 프로젝트 경험

### 아주대학교 — 메타버스기획 마이크로전공 (부전공)

`2021.03 ~ 2026.02 (예정)`
메타버스 플랫폼에서 상호작용 콘텐츠를 제작했습니다.

- 메타버스 플랫폼 콘텐츠 제작 경험

### 스파르타 내일배움캠프 — Unity 게임개발자 양성과정

`2023.09 ~ 2024.02`
Unity 기반 게임 개발 역량을 확장했습니다.

- Unity 기반 게임 개발 프로젝트 경험

### 구름(goorm) — 군장병 AI/SW 역량강화

`2023.03 ~ 2023.12`
HTML, CSS, JavaScript에 대한 기초 감각을 익혔습니다.

- 프론트엔드 기초 감각 확장

### 코드잇 — 대학생 코딩캠프

`2021.03 ~ 2021.04`
프로그래밍에 대한 기초 이해와 웹 개발 전반에 대한 감각을 익혔습니다.

- 프로그래밍에 대한 기초 이해 확장

---

## 03. 주요 프로젝트 (Main Projects)

> 카테고리: Web Service · Data/AI · Game · AR/XR · Ops

| # | 프로젝트 | 카테고리 | 상태 | 태그 |
|---|----------|----------|------|------|
| 1 | **득근득근 (MuscleUp)** | Web | 운영중 | FullStack, JWT, AWS, Security |
| 2 | **aClub** | Web | 운영중 | WebService, Operations, UX, Analytics |
| 3 | **아주대학교 총학생회** | Web | 운영중 | WebService, Operations, UX, Analytics |
| 4 | **FestFlow** | Web | 완료 | React, SSE, Spring Boot, PWA |
| 5 | **MyStock-Desk** | Data/AI | 완료 | React, Spring Boot, AI, Recharts |
| 6 | **Ajou Campus Foodmap** | Web | 완료 | FullStack, OAuth, Workflow |
| 7 | **수어지구** | Web | 완료 | Spring Boot, Firebase, 3D Avatar |
| 8 | **MyWave** | Data/AI | 완료 | React, TypeScript, Recharts |
| 9 | **TSEROF** | Game | 운영중 | Unity, GameDev, 3D |
| 10 | **아주대탐험** | Game | 완료 | Unity, AI, GameSystem |
| 11 | **DarkLab** | Game | 완료 | Unity, C#, Cinemachine, Horror |
| 12 | **The Other Side (VR)** | AR/XR | 완료 | VR, XR, Unity |
| 13 | **INTO MONSTER POINT (AR)** | AR/XR | 완료 | AR, Unity, Shooter |

**한 줄 설명**

1. **득근득근 (MuscleUp)** — 운영을 전제로 인증·보안·배포까지 설계한 피트니스 커뮤니티 풀스택 서비스
2. **aClub** — 실사용 운영에서 '문의 감소·참여 동선 명확화'를 목표로 만든 운영형 웹 서비스
3. **아주대학교 총학생회** — 반복 문의를 줄이고 '공지→확인→신청' 동선을 명확히 만든 운영형 웹 서비스
4. **FestFlow** — 대학 축제 운영자를 위한 실시간 부스 관리 시스템
5. **MyStock-Desk** — 거래 기록 기반 포트폴리오 분석과 AI 체크리스트를 제공하는 투자 기록 서비스
6. **Ajou Campus Foodmap** — 세션 기반 OAuth 로그인과 맛집 등록 플로우를 구현한 캠퍼스 지도 서비스
7. **수어지구** — 수어 아바타를 활용한 학습 및 표현 서비스
8. **MyWave** — 개인 자산 흐름을 한눈에 보는 금융 관리 대시보드 (컨셉/설계)
9. **TSEROF** — 출시·배포까지 완주한 3D 액션 플랫폼 게임 프로젝트
10. **아주대탐험** — 스킬·AI·이벤트 시스템을 통합 구현한 캐주얼 액션 어드벤처 게임
11. **DarkLab** — 1인칭 탐색 기반 3D 공포 어드벤처 게임 프로토타입
12. **The Other Side (VR)** — XR Interaction Toolkit 기반 VR 공포 퍼즐 탈출 게임
13. **INTO MONSTER POINT (AR)** — AR Plane 스캔 기반 전투 공간 생성 슈터 데모 프로젝트

---

## 04. 사이드 프로젝트 (Sub Projects)

| 프로젝트 | 설명 | 링크 |
|----------|------|------|
| 고양이로부터 지켜라 | 타워 디펜스 게임 1인 개발 | Demo |
| 루탄의 카드 게임 | 덱 빌딩 카드 게임 개발 | GitHub, Demo |
| 내 꿈이 현실의 버그에 침식당하기 시작해서 위험해 | 잠든 개발자가 꿈속 코드 세계에서 버그를 해결하는 게임 | GitHub, Demo, Notion |
| 스파르타 던전 배틀 (Text 게임) | 나만의 캐릭터를 생성하고, 그 캐릭터를 활용한 텍스트 게임 | GitHub, Demo, Notion |
| Fossil Runner | 섬에서 자원을 모아 성장하고, 용을 처치해 현실로 돌아가는 게임 | GitHub, Demo |
| MOVYDICK | 이더리움 고래 활동 추적 및 매매 시점 예측 시스템 개발 | Demo, Notion |
| NovelKub | NPC 단서 수집 기반 살인사건 추리 게임 | GitHub, Demo, Notion |
| time rewinder | Godot 기반 퍼즐 게임 개발 | Demo |
| 불빛아래 | AI 디자인을 적용한 3D 공포 게임 | GitHub, Demo |
| 경복궁을 지켜라 | 로블록스 기반으로 제작한 경복궁 복원 게임 | Demo |
| 페스트러너 | 곤충을 피해 도망가는 AR 기반 러너 게임 | GitHub, Demo |

---

## 05. 개발 기록 (Development Records)

- **개발 기록** — 개발 중 인상 깊었던 문제와 배운 점, 그날의 고민을 짧게 정리합니다.
- **개발 개념과 구조 학습 정리** — 핵심 개념과 구조를 중심으로 학습 내용을 정리합니다.
- **코딩 테스트 문제 풀이 기록** — 문제 풀이 과정과 접근 방식을 정리합니다.

---

## 06. 일하는 태도 (Values I Believe In)

- **소통** — 생각과 상황을 공유하며 방향을 맞춥니다.
- **협력** — 역할을 존중하며 함께 더 나은 결과를 만듭니다.
- **성실** — 작은 기록과 반복을 통해 꾸준히 성장합니다.
- **도전** — 익숙함에 머무르지 않고 새로운 시도를 선택합니다.

---

## 07. About Me

- 작은 기능도 끝까지 다듬어 '운영 가능한 상태'로 만듭니다.
- 배포 후 생기는 HTTPS·CORS 같은 문제를 로그/설정/네트워크까지 파고들어 해결해왔습니다.
- 저는 운영과 사용자 소통까지 이어져야 비로소 '완성된 개발'이라고 생각합니다.
- 웹이 주력이지만, Unity XR 경험으로 인터랙션 영역도 다룰 수 있습니다.

---

## 08. 연락처 (Contact)

협업/인턴/프로젝트 제안 모두 환영합니다. 가장 빠른 연락은 이메일로 부탁드립니다.

- ✉️ **Email:** toadsam@naver.com
- 🔗 **GitHub:** https://github.com/toadsam

---
---

# 주요 프로젝트 상세 (Project Deep Dives)

> 각 프로젝트의 문제 정의 → 가설 → 아키텍처 → 기술 의사결정 → 핵심 코드 → 트러블슈팅 → 회고까지의 상세 내용입니다. (원페이지 뷰의 원본 데이터)

---

## 🏋️ 득근득근 (MuscleUp)

> GAMIFIED FITNESS · 3-TIER FULL-STACK

**TL;DR**

| | |
|---|---|
| 무엇을 | 운동 기록을 게임처럼 지속하게 만드는 피트니스 커뮤니티 플랫폼 |
| 왜 | 운동 앱은 기록은 되지만 '다시 오게' 만드는 장치가 약함 |
| 결과 | REST 백엔드 + Socket.IO 실시간 서버를 분리한 3-tier 구조 |
| 내 역할 | 1인 풀스택 — 기획·프론트·백엔드·실시간 서버 |

**메타**
- **팀:** 개인 프로젝트 (풀스택 1인)
- **역할:** 핵심 루프 설계 · 프론트 · 백엔드 · 실시간 서버
- **스택:** React 19 · TS · TanStack Query · Spring Boot 3.5 · Socket.IO · JWT/OAuth · S3
- **실행:** 3-tier(FE:5173 · BE:8080 · Realtime:4001)
- **저장소:** https://github.com/toadsam/Ajou_MuscleUp

**문제**
운동 앱은 기록하는 순간엔 유용하지만 꾸준히 돌아오게 만드는 장치가 약하다. 기록이 캐릭터 성장·랭킹·실시간 커뮤니티로 즉시 연결되는 경험이 필요했고, 자주 바뀌는 실시간 상태를 REST와 분리해야 했다.

**가설**
"운동 기록을 캐릭터 성장·랭킹·라운지로 즉시 연결하고, 빈번히 바뀌는 실시간 상태(위치·채팅·이모트)는 Socket.IO 서버로 분리하면 — 사용자가 '다시 오는' 이유가 생기면서 서버도 감당 가능하다."

**아키텍처**
- **View** — React 19 + TanStack Query: 홈·라운지·랭킹·AI (PWA)
- **API** — Spring Boot 3.5: 16 도메인 · JWT/OAuth · S3 · PDFBox
- **Realtime** — Node + Socket.IO: 라운지 위치·채팅·이모트 (:4001)
- **DB** — MySQL / PostgreSQL: 유저·기록·크루·이벤트

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 실시간 | Socket.IO 분리 서버 | 빈번한 상태(위치·채팅)를 REST와 분리 ↔ REST 폴링(부하·지연) |
| 인증 | JWT access/refresh + OAuth | 무상태·간편 로그인·쿠키/Bearer 겸용 ↔ 자체 세션(스케일↓) |
| 서버상태 | TanStack Query | 캐시·재검증으로 서버 상태 일원화 ↔ 수동 fetch(중복 상태) |
| 파일 | 로컬/S3 추상화 | 개발은 로컬, 배포는 S3로 전환 ↔ S3 고정(로컬 불편) |

**핵심 코드**

```ts
// server.ts (realtime) — 라운지 상태는 Socket.IO 서버가 전담, REST와 분리
io.on("connection", (socket) => {
  socket.on("move", (p) => room.update(socket.id, p)); // 위치
  socket.on("chat", (m) => io.to(room.id).emit("chat", m));
  socket.on("disconnect", () => room.leave(socket.id));
});
```

```ts
// axios-interceptor.ts — 401이면 refresh로 토큰 갱신 후 재요청
api.interceptors.response.use(ok, async (err) => {
  if (err.response?.status === 401) {
    await api.post("/api/auth/refresh"); // access 재발급
    return api(err.config); // 원요청 재시도
  }
  throw err;
});
```

**트러블슈팅**
- **자주 바뀌는 라운지 상태를 REST로 처리하기 어려웠다** — 위치·채팅·이모트처럼 초당 여러 번 바뀌는 상태를 REST로 다루니 지연·부하가 컸다. → 실시간 상태만 담당하는 Socket.IO 서버(:4001)를 REST 백엔드와 분리해 각자 잘하는 일을 하도록 아키텍처를 나눴다.
- **브라우저·배포 환경에서 인증 방식이 달랐다** — 쿠키 기반과 Bearer 토큰 기반이 섞여 로그인 유지가 불안정했다. → 요청 인터셉터에서 access 자동 첨부 + 401 시 refresh 재발급, 백엔드가 쿠키·Bearer를 모두 처리하도록 했다.

**성과 지표:** 실행 단위 3 · API 도메인 16 · 실시간 서버 Socket.IO · 풀스택 1인

**회고 (KPT)**
- Keep: 실시간을 REST와 분리한 아키텍처 / 인증을 환경 차이까지 흡수하게 설계
- Problem: AI 인바디 정확도 검증 부족 / 온보딩이 다소 복잡
- Try: 웨어러블 연동 / 크루 챌린지 고도화

**배운 점**
기능을 많이 만드는 것보다 '실시간은 실시간 서버로, 데이터는 REST로'처럼 책임을 나누는 아키텍처 판단이 서비스 안정성을 좌우한다는 걸 배웠다.

**기술 스택:** React 19 · TypeScript · TanStack Query · Spring Boot 3.5 · Socket.IO · JWT/OAuth · AWS S3

---

## 🎪 FestFlow

> REALTIME FESTIVAL OPS · FULL-STACK WEB APP

**TL;DR**

| | |
|---|---|
| 무엇을 | 대학 축제 부스를 실시간으로 운영·관제하는 웹앱 (Fest-A) |
| 왜 | 수십 개 부스의 상태·혼잡도·공지를 실시간 공유할 도구가 없었음 |
| 결과 | 7개 SSE 채널 + 실제 지도 + GPS 지오펜싱으로 실시간 운영 |
| 내 역할 | 1인 풀스택 (프론트 전체 + Spring Boot 백엔드) |

**메타**
- **팀:** 개인 프로젝트 (풀스택 1인)
- **역할:** 프론트 전체 · SSE·GPS 백엔드
- **스택:** React · Vite · Leaflet · PWA · Spring Boot 3 · JWT · SSE · MySQL
- **저장소:** https://github.com/toadsam/FestFlow

**문제**
대학 축제는 수십 개 부스가 동시에 운영되는데, 혼잡도·품절·공지·분실물 같은 상태를 실시간으로 공유하고 역할별로 접근할 도구가 없어 전화·메신저로 처리하고 있었다.

**가설**
"상태 변화를 종류별 SSE 채널로 분리해 즉시 푸시하고, 실제 지도 위에서 GPS 반경으로 부스를 판정하면 — 관리자·스태프·방문자가 같은 현황을 실시간으로 공유할 수 있다."

**아키텍처**
- **View** — React + Leaflet(PWA): 지도·부스 카드·관리 대시보드
- **Stream** — 7× SseEmitter: /api/stream/congestion·booths·notices·…
- **Geo** — BoothService: GPS 로그 기반 반경 80m 지오펜싱
- **DB** — MySQL 8: 부스·예약·GPS 로그·스태프

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 실시간 | SSE(채널 분리) | 단방향 푸시에 충분, 종류별 구독으로 부하 분리 ↔ WebSocket(양방향 과함) |
| 지도 | React Leaflet + OSM | 오픈소스·비용 0·커스텀 마커 ↔ Google Maps(비용) |
| 위치 | 반경 80m 지오펜싱 | 현장 도보 판정에 현실적 ↔ 정확 좌표 매칭(오차↑) |
| 인증 | JWT 3등급 | 관리자/스태프/방문자 무상태 권한 ↔ 세션(스케일↓) |

**핵심 코드**

```java
// StreamController.java — 상태 종류별로 SSE 채널을 분리 (7개 엔드포인트)
@RestController @RequestMapping("/api/stream")
public class StreamController {
  @GetMapping(value="/congestion", produces=TEXT_EVENT_STREAM_VALUE)
  public SseEmitter congestion() { return service.subscribeCongestion(); }
  // + booths / notices / lost-items / reservations / staff / events
}
```

```java
// BoothService.java — GPS 로그 기준 반경 80m로 부스 근접 판정
private static final double BOOTH_RADIUS_METERS = 80.0;
boolean near = distance(gps, booth) <= BOOTH_RADIUS_METERS;
```

**트러블슈팅**
- **모바일에서 SSE 연결이 끊기면 현황이 멈췄다** — 네트워크가 불안정하면 EventSource가 끊긴 채 복구되지 않았다. → 끊김 감지 시 재연결하고, 재연결 직후 스냅샷을 다시 받아 상태를 동기화했다.
- **모든 상태를 한 채널로 밀면 부하가 컸다** — 혼잡도·공지·부스를 한 스트림에 섞으니 불필요한 이벤트까지 모두가 받았다. → 상태 종류별로 SSE 채널을 7개로 분리해 필요한 것만 구독하게 했다.

**성과 지표:** SSE 채널 7 · GPS 지오펜싱 80m · 권한 등급 3 · 풀스택 1인

**회고 (KPT)**
- Keep: 상태를 종류별 채널로 분리한 설계 / 실제 지도 + GPS로 현장성 확보
- Problem: 부하 테스트 부족 / 마커가 많을 때 렌더 최적화 미흡
- Try: 부스 매출 집계 / 혼잡도 히트맵

**배운 점**
'실시간'이라고 무조건 WebSocket이 아니라, 단방향 푸시에는 SSE를 종류별로 나누는 게 더 단순하고 안정적이었다. 위치 판정은 '정확함'보다 '현장에서 말이 되는가'가 중요했다.

**기술 스택:** React · Vite · React Leaflet · PWA · Spring Boot 3 · JWT · SSE · MySQL

---

## 📈 MyStock-Desk

> INVESTMENT PORTFOLIO · FULL-STACK WEB APP

**TL;DR**

| | |
|---|---|
| 무엇을 | 거래 기록 기반 포트폴리오 분석 + AI 체크포인트 웹앱 (StockFlow) |
| 왜 | 수익률·자산 비중·뉴스 영향을 한 곳에서 보기 어려움 |
| 결과 | 증권사 실연동 없이 입력 거래만으로 포트폴리오를 계산·분석 |
| 내 역할 | 1인 풀스택 — React 프론트 + Spring Boot 백엔드 |

**메타**
- **팀:** 개인 프로젝트 (풀스택 1인)
- **역할:** 도메인 설계 · 프론트 · 백엔드 전부
- **스택:** React · TS · Vite · Spring Boot · JPA · H2/MySQL · Recharts
- **저장소:** https://github.com/toadsam/MyStock-Desk

**문제**
투자 기록을 직접 관리하면 수익률·자산 비중을 한눈에 파악하기 어렵고, 뉴스가 내 종목에 어떤 영향을 주는지 판단할 도구가 부족하다. 실제 증권사 연동 없이도 '내가 입력한 데이터'만으로 분석이 되어야 했다.

**가설**
"거래 기록을 도메인별로 나눠 서버에서 집계하고, AI는 '판단'이 아니라 '정리' 역할로 한정하며, 외부 시세가 실패해도 Demo Provider로 폴백하면 — 실연동 없이도 신뢰할 수 있는 포트폴리오 분석을 제공할 수 있다."

**아키텍처**
- **View** — React + Recharts: 포트폴리오·수익률·뉴스·리서치 화면
- **API** — Spring Boot REST: portfolio · transaction · stock · research 등 13 도메인
- **Market** — MarketDataProvider: Yahoo Finance ↔ Demo(seed) 폴백 인터페이스
- **DB** — H2 / MySQL: JPA로 거래·보유·리서치 영속화

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 백엔드 | Spring Boot | 도메인 분리·검증·보안에 강함 ↔ Express(타입·검증 약함) |
| AI 역할 | 정리형 | 투자 추천의 신뢰·법적 리스크 회피 ↔ 추천형(리스크↑) |
| 시세 | Provider 추상화 | Yahoo 실패 시 Demo로 폴백 ↔ 단일 연동(장애 시 빈 화면) |
| 차트 | Recharts | 선언적 구현·React 친화 ↔ Chart.js(명령형) |

**핵심 코드**

```java
// MarketDataProvider.java — 시세 소스를 인터페이스로 추상화, Yahoo/Demo 교체 가능
public interface MarketDataProvider {
  List<StockQuoteSnapshot> fetch(List<String> symbols);
}
// YahooFinanceMarketDataProvider · DemoMarketDataProvider 가 구현
// 설정(stockflow.market-data.provider)으로 스위치 → 실패 시 Demo 폴백
```

```java
// PortfolioService.java — 요청마다 스냅샷을 갱신한 뒤 포트폴리오 반환
@Transactional
public PortfolioDto getPortfolio() {
  Portfolio portfolio = findPortfolio();
  portfolioSnapshotService.refresh(portfolio); // 최신 시세 반영
  return PortfolioDto.from(portfolio);
}
```

**트러블슈팅**
- **외부 시세/뉴스가 실패하면 화면이 비었다** — Yahoo Finance·뉴스 RSS 호출이 실패하면 대시보드가 빈 화면이 됐다. → MarketDataProvider를 인터페이스로 두고 DemoMarketDataProvider(seed)로 폴백해, 외부가 죽어도 화면 구조를 유지했다.
- **AI가 추천처럼 답하려 했다** — '사세요/파세요' 류 답이 섞여 신뢰를 해쳤다. → 리서치 응답을 '리스크·뉴스 변화 정리' 스키마로 한정하고, 판단 주체를 사용자로 유지했다.

**성과 지표:** 백엔드 도메인 13 · 주요 페이지 9 · 시세 Provider 2 · 풀스택 1인

**회고 (KPT)**
- Keep: 도메인을 먼저 나누고 화면을 붙인 순서 / AI 역할을 '정리'로 제한 / Provider 폴백 설계
- Problem: 테스트 코드 부족 / 프론트 상태 관리가 후반에 복잡해짐
- Try: 실시간 시세 연동 / 포트폴리오 자동 갱신

**배운 점**
AI를 '판단 주체'가 아니라 '정리 도구'로 제한했을 때 오히려 신뢰가 올라갔다. 외부 의존은 반드시 폴백을 두어야 화면이 살아있다는 걸 배웠다.

**기술 스택:** React · TypeScript · Vite · Spring Boot · Spring Data JPA · H2/MySQL · Recharts

---

## 🌊 MyWave

> PERSONAL FINANCE · WEB APP (컨셉/설계)

**TL;DR**

| | |
|---|---|
| 무엇을 | 소비·투자를 하나의 '흐름'으로 보는 개인 자산 관리 웹앱 |
| 왜 | 기존 앱은 거래를 나열만 함 → 누적 흐름 + 목표선으로 재해석 |
| 결과 | 거래를 누적 흐름 + 목표선으로 보여주는 대시보드 UX 설계·구현 |
| 내 역할 | 기획 · UX 설계 · 프론트엔드 전체 (1인 프로젝트) |

**메타**
- **팀:** 개인 프로젝트 (기획·디자인·개발 1인)
- **역할:** 서비스 기획 · UX 설계 · 프론트엔드 전체
- **스택:** React · TypeScript · Recharts · Zustand · Tailwind
- **성격:** 개인 컨셉·설계 프로젝트 (공개 리포 없음)

**문제**
기존 가계부·금융 앱은 거래 내역을 단순 표로 나열한다. 숫자는 많지만 정작 '내 돈이 어디로 흘러가는지', '목표까지 얼마나 왔는지'는 한눈에 보이지 않았다.

**가설**
"거래를 누적 흐름 + 목표선으로 바꿔 보여주면, 사용자가 자신의 재정 상태를 즉시 직관적으로 이해할 것이다."

**아키텍처**
- **View** — React 컴포넌트: 대시보드/차트/폼
- **State** — Zustand store: 거래·목표·필터 전역 상태
- **Domain Logic** — Hooks: useCashFlow · goalProgress · 통화 정규화
- **Data Source** — I/O: REST API ↔ localStorage 캐시

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 차트 | Recharts | 선언적 API로 빠른 구현, SVG 커스터마이즈 ↔ visx(러닝커브↑), D3(과한 복잡도) |
| 상태관리 | Zustand | 보일러플레이트 최소, 선택적 구독 ↔ Redux(무거움), Context(전역 리렌더) |
| 스타일 | Tailwind | 반응형 빠르게, 디자인 일관성 ↔ CSS Modules, styled-components |
| 데이터 | API + localStorage | MVP는 로컬로 빠르게, 이후 API 전환 용이 ↔ 처음부터 풀백엔드(속도↓) |

**핵심 코드**

```ts
// useCashFlow.ts — 거래 배열 → 일별 누적 잔액 시계열로 변환
export function useCashFlow(txns: Transaction[]) {
  return useMemo(() => {
    let running = 0;
    return txns
      .sort((a, b) => a.date - b.date)
      .map((tx) => {
        running += normalizeToKRW(tx);
        return { date: tx.date, balance: running };
      });
  }, [txns]);
}
```

```tsx
// FlowChart.tsx — 누적 흐름 위에 목표선을 겹쳐 '현재 위치'를 시각화
<AreaChart data={flow}>
  <defs>
    <linearGradient id='wave'>
      <stop offset='0%' stopColor={accent} stopOpacity={0.4} />
      <stop offset='100%' stopOpacity={0} />
    </linearGradient>
  </defs>
  <Area dataKey='balance' stroke={accent} fill='url(#wave)' />
  <ReferenceLine y={goal} label='목표' strokeDasharray='4 4' />
</AreaChart>
```

**트러블슈팅**
- **실시간 갱신마다 차트가 깜빡였다** — 거래 추가 시 차트 데이터 배열이 매번 새 참조로 만들어져 Recharts가 전체를 리마운트했다. → 흐름 데이터를 useMemo로 안정화하고 key를 의미 단위로 고정해 리마운트를 제거했다.
- **통화가 섞인 거래의 합산** — 해외 결제 등으로 통화가 섞여 단순 합산 시 금액이 왜곡됐다. → 거래 '시점'의 환율로 KRW에 정규화한 뒤 누적, 환율은 스냅샷으로 저장해 과거 값이 흔들리지 않게 했다.

**성과 지표:** 핵심 화면 4 · 핵심 UX 컨셉 흐름+목표선 · 기획·UX·프론트 1인

**회고 (KPT)**
- Keep: 문제→가설→UX 컨셉으로 이어지는 설계 흐름 / 데이터를 '흐름·목표' 맥락으로 재구성한 관점
- Problem: 초기 데이터 모델을 너무 일찍 고정 / 테스트 커버리지 부족
- Try: 백엔드 연동해 실데이터 검증 / 온보딩 A/B 테스트

**배운 점**
데이터를 '보여주는 것'과 '읽히게 하는 것'은 다르다. 같은 거래 데이터라도 흐름·목표라는 맥락을 입히자 이해가 완전히 달라졌고, 기술 선택은 '멋짐'이 아니라 '이 문제에 맞는가'로 판단해야 한다는 걸 배웠다.

**기술 스택:** React · TypeScript · Recharts · Zustand · TailwindCSS · Vite · PWA

---

## 🎓 aClub (ACLUB)

> CLUB DISCOVERY · TEAM FRONTEND

**TL;DR**

| | |
|---|---|
| 무엇을 | 동아리 탐색·모집을 한 곳에서 하는 플랫폼 프론트엔드 (AjouClub) |
| 왜 | 동아리 정보가 여러 채널에 흩어져 탐색·지원이 번거로움 |
| 결과 | 탐색→상세→지원까지 한 흐름으로 처리하는 FE 완성 |
| 내 역할 | 3인 프론트엔드 팀 중 1인 (React + TS) |

**메타**
- **팀:** 프론트엔드 3인 (본인 포함)
- **역할:** 탐색·상세·마이 등 화면·훅 담당
- **스택:** React · TypeScript · Vite · Tailwind · React Router · axios
- **배포:** GitHub Pages (커스텀 도메인 · CNAME)
- **저장소:** https://github.com/aClub2026/FE

**문제**
동아리 정보는 에브리타임·인스타·단톡 등 여러 채널에 흩어져 있어 학생들이 원하는 동아리를 탐색하고 지원 현황을 관리하기 어려웠다.

**가설**
"탐색·필터·상세·지원을 한 흐름으로 묶고, 데이터 요청을 도메인별 커스텀 훅으로 모으면 — 학생은 한 곳에서 동아리를 찾고, 팀은 화면을 병렬로 개발할 수 있다."

**아키텍처**
- **Route** — React Router: 탐색/상세/필터/마이/관리 라우팅
- **Data** — Custom Hooks: useClubs · useRecruitments · useMypageData …
- **API** — axios 모듈: auth · club · recruitment · user
- **UI** — 공통 컴포넌트: Card · Chip · Field · Modal 재사용

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 빌드 | Vite | 빠른 HMR·경량 ↔ CRA(느림) |
| 타입 | TypeScript | 여러 명이 만지는 화면의 안정성 ↔ JS(런타임 오류↑) |
| 데이터 | 도메인 커스텀 훅 | 요청 로직 재사용·화면 병렬 개발 ↔ 컴포넌트마다 fetch(중복) |
| 권한 | RequireAuth/AdminRoute | 라우트 단위 보호를 명시적으로 ↔ 화면 안에서 분기(누락 위험) |

**핵심 코드**

```ts
// useClubs.ts — 동아리 데이터 요청을 훅으로 모아 화면에서 재사용
export function useClubs(params: ClubQuery) {
  const [data, setData] = useState<Club[]>([]);
  useEffect(() => { club.list(params).then(setData); }, [params]);
  return data;
}
```

```tsx
// RequireAuth.tsx — 로그인 여부로 보호 라우트 처리
function RequireAuth({ children }) {
  const { user } = useAuth();
  return user ? children : <Navigate to='/login' replace />;
}
```

**트러블슈팅**
- **여러 화면이 같은 데이터를 제각각 불렀다** — 탐색·상세·마이페이지가 동아리 데이터를 각자 fetch해 로직이 중복되고 상태가 어긋났다. → 도메인별 커스텀 훅으로 요청·상태를 캡슐화해 같은 훅을 재사용하게 했다.
- **로그인/관리자 화면 접근 제어가 흩어졌다** — 권한 체크를 화면마다 넣다 보니 누락이 생겼다. → RequireAuth·AdminRoute 보호 라우트로 라우팅 계층에서 한 번에 제어했다.

**성과 지표:** 커스텀 훅 6+ · FE 팀 3인 · TypeScript 적용 · GitHub Pages 배포

**회고 (KPT)**
- Keep: 데이터 요청을 훅으로 모은 것 / 보호 라우트로 권한 일원화 / 공통 컴포넌트 재사용
- Problem: 디자인 시스템이 후반에 정리됨 / 테스트 부족
- Try: 백엔드 연동 안정화 / 검색 고도화

**배운 점**
팀 프론트엔드에서는 '같은 데이터를 어떻게 한 번만 정의해 재사용할까'가 협업 속도를 좌우했다. 커스텀 훅과 보호 라우트가 그 답이었다.

**기술 스택:** React · TypeScript · Vite · TailwindCSS · React Router · axios

---

## 🏫 아주대학교 총학생회 (ajouchong)

> STUDENT COUNCIL · TEAM SPA + DEPLOY

**TL;DR**

| | |
|---|---|
| 무엇을 | 아주대 총학생회 공식 웹사이트 프론트엔드 |
| 왜 | 공지·Q&A·복지·자료가 여러 채널에 흩어져 있었음 |
| 결과 | 정보를 한 SPA에 모아 Docker + Nginx로 실제 배포 |
| 내 역할 | 3인 팀 프론트엔드 · 배포 담당 |

**메타**
- **팀:** 총학생회 IT · 프론트 3인
- **역할:** SPA 라우팅·화면 · Docker/Nginx 배포
- **스택:** React 18 · CRA · JavaScript · React Router · Docker · Nginx
- **배포:** Docker + Nginx (실서비스 구성)
- **저장소:** https://github.com/toadsam/ajouchong-web

**문제**
총학생회 정보가 인스타·공지 등 여러 채널에 분산되어 학생들이 공지·Q&A·복지·자료를 한 곳에서 확인하기 어려웠고, 실제로 배포해 '쓰이는 서비스'로 만들어야 했다.

**가설**
"핵심 정보를 하나의 SPA로 모으고 Docker + Nginx로 환경을 고정해 배포하면 — 학생 정보 접근성이 오르고, '내 컴퓨터에선 되는데' 문제 없이 안정적으로 운영된다."

**아키텍처**
- **Route** — React Router SPA: 공지/Q&A/자료/복지 라우팅
- **Auth** — AuthContext: 로그인 상태 전역·ProtectedRoute
- **Serve** — Nginx: 정적 서빙 · SPA try_files 폴백
- **Ship** — Docker: 빌드·실행 환경 고정

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 구조 | SPA | 빠른 화면 전환 ↔ MPA(새로고침) |
| 서빙 | Nginx | 정적+폴백 안정적 ↔ Node 서버(과함) |
| 배포 | Docker | 환경 일관성 확보 ↔ 수동 배포(실수↑) |
| 인증 | Context | 단순 로그인 상태에 충분 ↔ 라이브러리(과함) |

**핵심 코드**

```nginx
# nginx.conf — SPA 새로고침 404 방지, try_files 폴백
location / {
  try_files $uri $uri/ /index.html; # 없는 경로는 SPA로
}
```

```js
// ProtectedRoute.js — 로그인 상태에 따른 보호 라우트
function ProtectedRoute({ children }) {
  const { user } = useContext(AuthContext);
  return user ? children : <Navigate to='/login' />;
}
```

**트러블슈팅**
- **새로고침하면 404가 떴다** — SPA 라우트에서 새로고침 시 Nginx가 실제 파일을 못 찾아 404를 반환했다. → try_files로 모든 경로를 index.html로 폴백시켜 클라이언트 라우터가 처리하게 했다.
- **로컬은 되는데 서버에선 달랐다** — 환경 차이로 빌드·실행이 다르게 동작했다. → Docker로 빌드·실행 환경을 고정해 '내 컴퓨터에선 되는데' 문제를 없앴다.

**성과 지표:** 실배포 Docker+Nginx · 팀 협업 3인 · SPA 라우팅 · 새로고침 404 = 0

**회고 (KPT)**
- Keep: 실제 배포까지 끝낸 경험 / 환경을 Docker로 고정
- Problem: 모바일 최적화 미흡 / 접근성 보강 필요
- Try: 알림 기능 / 관리자 통계

**배운 점**
'만드는 것'을 넘어 '배포해서 진짜 쓰이게 하는 것'까지 해보며 Docker·Nginx 같은 운영 지식과 SPA 배포의 함정(폴백)을 몸으로 배웠다.

**기술 스택:** React 18 · JavaScript · React Router · Docker · Nginx

---

## 🤟 수어지구 (Sign-Language)

> SIGN LANGUAGE LEARNING · TEAM (BACKEND)

**TL;DR**

| | |
|---|---|
| 무엇을 | 3D 아바타로 배우고 표현하는 수어 학습 서비스 (파란학기 과제) |
| 왜 | 수어를 '번역 대상'이 아니라 '읽고 익히는 언어'로 학습할 도구가 부족 |
| 결과 | 동작 보고 의미 맞히기 + 텍스트→수어 변환 시제품 (30+ 단어) |
| 내 역할 | 4인 팀의 백엔드 — Spring Boot 서버·API·데이터 처리 |

**메타**
- **팀:** 4인 (FE 1 · BE 2 · 3D 아바타 1)
- **역할:** 백엔드 — 서버 구축·API 설계·입력 데이터 처리
- **스택:** Spring Boot(Gradle) · Firebase/Firestore · Cloud Storage · React · Vite
- **배포:** GitHub Pages (프론트) · /api/health 상태 체크
- **저장소:** https://github.com/toadsam/Sign-Language

**문제**
기존 수어 플랫폼은 대부분 '문장을 넣으면 아바타가 수어로 보여주는' 구조라 이미 수어를 아는 사람을 전제한다. 수어를 처음 접하는 사람이 동작을 보고 의미를 익히는 '읽기 학습' 도구가 부족했다.

**가설**
"아바타가 동작을 먼저 보여주고 사용자가 의미를 입력하는 양방향 구조 + 단어-동작 매핑을 단일 데이터 소스로 두면 — 처음 배우는 사람도 반복으로 수어를 '읽는' 감각을 기를 수 있다."

**아키텍처**
- **View** — React + 3D Avatar: 퀴즈·변환 화면 (Vite)
- **API** — Spring Boot: 학습·정답판정·변환 API (담당)
- **Data** — Firebase/Firestore: 단어·동작 매핑 · Cloud Storage
- **Contract** — 단일 데이터 계약: 단어↔동작 키를 한 소스로 통일

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 백엔드 | Spring Boot | API·검증 구조에 적합 ↔ Flask(타입 약함) |
| 데이터 | Firestore + Storage | 단어·에셋을 빠르게 저장·확장 ↔ 자체 DB 구축(공수↑) |
| 정답판정 | 표기 정규화 비교 | 공백·표기 흔들림 흡수 ↔ 완전 일치(오답↑) |
| 협업 | API 계약 우선 | FE·아바타와 병렬 작업 ↔ 후순위(병목) |

**핵심 코드**

```java
// QuizController.java — 정답을 정규화해 표기 차이를 흡수
public Result check(Long id, String answer) {
  var word = repo.findById(id);
  boolean ok = normalize(word.getMeaning())
             .equals(normalize(answer)); // 공백·표기 정규화
  return new Result(ok, word.getMeaning());
}
```

```
// word-contract — 단어↔동작 키를 한 소스로 정의, FE·아바타가 그대로 사용
{ id, meaning, signKey } // signKey = 아바타 애니메이션 키
// API가 signKey를 내려주면 아바타가 해당 동작 재생
```

**트러블슈팅**
- **같은 의미를 다르게 입력하면 오답이 됐다** — '감사합니다'와 '감사 합니다'가 다른 답으로 처리됐다. → 공백·표기를 정규화한 뒤 비교해 의미가 같으면 정답 처리했다.
- **단어 데이터와 아바타 동작이 어긋났다** — 백엔드 단어와 아바타 애니메이션 키가 따로 관리돼 동작이 안 나오는 경우가 있었다. → 단어-동작 매핑을 단일 소스로 두고 API가 그 키를 그대로 내려주도록 계약을 통일했다.

**성과 지표:** 팀 4인(BE 담당) · Firestore 데이터 · 양방향(학습+변환) · 단어 30+

**회고 (KPT)**
- Keep: API 계약을 먼저 정해 병렬 작업 / 정답 정규화로 UX 개선
- Problem: 단어 수가 적음 / 실사용자 테스트 부족
- Try: 단어 100개+ 확장 / 동작 인식(카메라) 도입

**배운 점**
프론트·아바타·백엔드가 얽힌 협업에서 '데이터 계약을 단일 소스로 통일하는 것'이 가장 중요했다. 백엔드는 결국 신뢰할 수 있는 계약을 내려주는 역할이었다.

**기술 스택:** Spring Boot · Firebase/Firestore · React · Vite · 3D Avatar

---

## 🏃 아주대탐험 (Ajou_Mini_Game)

> 2D CASUAL RUNNER · PHASER 3 (SOLO)

**TL;DR**

| | |
|---|---|
| 무엇을 | 아주대 캠퍼스 테마 2D 캐주얼 러닝 게임 (아주분투) |
| 왜 | 대학생이 공감할 가벼운 웹 게임을 만들고 싶었음 |
| 결과 | Scene/Object/System으로 모듈화 + 자동 플레이테스트 하네스 |
| 내 역할 | 기획·개발 전담 (1인) |

**메타**
- **팀:** 개인 프로젝트 (전담)
- **역할:** 기획·개발·플레이테스트 전부
- **스택:** TypeScript · Phaser 3 · Vite
- **배포:** GitHub Pages / Vercel (정적)
- **저장소:** https://github.com/toadsam/Ajou_Mini_Game

**문제**
쉬는 시간에 잠깐 즐길, 대학생이 공감할 가벼운 웹 게임이 의외로 없었다. 캠퍼스 감성을 살리되(공식 로고 없이 도형 기반) 조작이 매끄럽고 끊기지 않는 러너가 필요했다.

**가설**
"게임을 Scene/Object/System 레이어로 나누고, 발판·아이템을 재사용(풀링)하면 — 혼자서도 유지보수 가능하고 끊기지 않는 러너를 만들 수 있다."

**아키텍처**
- **Scene** — Boot/Menu/Game/GameOver: 씬 단위 화면 흐름
- **Object** — Player/Platform/Item/Obstacle: 게임 오브젝트 클래스
- **System** — Spawner/Score/Background/Input: 생성·점수·배경·입력 매니저
- **Store** — localStorage: 최고 점수 저장

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 엔진 | Phaser 3 | 2D 웹게임 표준·빠른 개발 ↔ Canvas 직접(공수↑) |
| 구조 | Scene/Object/System 분리 | 1인 유지보수·확장 용이 ↔ 단일 파일(스파게티) |
| 충돌 | 스프라이트≠충돌박스 | 208×140 스프라이트에 38×58 바디로 판정 정확화 ↔ 스프라이트 전체 충돌(부정확) |
| 저장 | localStorage | 서버 없이 기록 유지 ↔ 백엔드(과함) |

**핵심 코드**

```ts
// Player.ts — 2단 점프, 지상/공중 점프 횟수로 제어
private jumpCount = 0;
private readonly maxJumps = 2;
jump() { if (this.jumpCount < this.maxJumps) { this.leap(); this.jumpCount++; } }
// 착지 시 jumpCount = 0 으로 리셋
```

```ts
// Player.ts (collision) — 큰 스프라이트에서 실제 몸통만 충돌 처리
// 스프라이트 프레임은 208×140, 몸통만 충돌
this.setSize(38, 58);
this.setOffset(85, 36); // 중앙 몸통 영역
```

**트러블슈팅**
- **큰 스프라이트가 실제보다 크게 부딪혔다** — 208×140 캐릭터 스프라이트 전체로 충돌 판정하니 안 닿았는데 죽는 억울한 판정이 났다. → 스프라이트와 충돌 바디를 분리해 중앙 몸통(38×58)만 충돌하도록 setSize/setOffset을 조정했다.
- **손으로 반복 테스트하기 어려웠다** — 점프·발판 간격 튜닝을 매번 손으로 확인하기 번거로웠다. → 플레이를 자동 실행해 스크린샷·로그(JSON)를 뽑는 플레이테스트 하네스를 만들어 반복 검증했다.

**성과 지표:** Scene/Obj/Sys 모듈 4·4·4 · 2단 점프 · 자동 플레이테스트 · 전담 1인

**회고 (KPT)**
- Keep: 게임을 레이어로 모듈화 / 자동 플레이테스트로 튜닝 검증
- Problem: 와이어 액션은 비활성(미완) / 사운드·콘텐츠 분량 부족
- Try: 리더보드·소셜 공유 / 와이어 액션 완성·스테이지 다양화

**배운 점**
1인 게임도 Scene/Object/System으로 나누면 유지보수가 완전히 달라졌고, 플레이 감을 '느낌'이 아니라 자동 플레이테스트 로그로 검증하니 튜닝이 훨씬 빨라졌다.

**기술 스택:** TypeScript · Phaser 3 · Vite

---

## 🔦 DarkLab

> FIRST-PERSON HORROR · UNITY (TEAM)

**TL;DR**

| | |
|---|---|
| 무엇을 | 1인칭 탐색 기반 3D 공포 어드벤처 프로토타입 (Threshold) |
| 왜 | 공포의 긴장감은 사운드보다 '공간 탐색·상호작용'에서 나옴 |
| 결과 | 탐색·상호작용·카메라 연출 + 3개국어 로컬라이즈 프로토타입 |
| 내 역할 | 3인 팀의 게임 프로그래밍 — 제어·상호작용·연출 |

**메타**
- **기간:** 2024-1 학기 (팀 프로젝트)
- **팀:** 3인 (프로그래밍 담당)
- **역할:** 플레이어 제어·상호작용·카메라 연출
- **스택:** Unity 2022.3.2f1 · C# · URP · Cinemachine · DOTween · Localization
- **저장소:** https://github.com/toadsam/DarkLab

**문제**
공포 게임의 긴장감은 단순한 점프스케어보다 공간 탐색과 오브젝트 상호작용에서 나온다. 이를 자연스럽게 구현하는 카메라 연출과 이벤트 흐름, 그리고 다국어 지원까지 필요했다.

**가설**
"레이캐스트 상호작용 + Cinemachine 시야 연출을 결합하면, 점프스케어 없이도 탐색 자체로 긴장감을 만들 수 있다."

**아키텍처**
- **Input** — Input System: 1인칭 이동·시점
- **Interact** — Raycast Interactor: IInteractable 조사·집기
- **Camera** — Cinemachine + DOTween: 전환·시야 제한 연출
- **Data** — ScriptableObject: 캐릭터·상태 · Localization/Addressables

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 상호작용 | Raycast | 직관적 '바라보는 것' 조사 ↔ 트리거 콜라이더(부정확) |
| 카메라 | Cinemachine | 블렌딩·연출 강력 ↔ 수동 카메라(공수↑) |
| 데이터 | ScriptableObject | 디자이너 친화·유지보수 ↔ 하드코딩(경직) |
| 렌더/다국어 | URP + Localization | 조명 연출 + 3개국어 대응 ↔ Built-in(연출↓) |

**핵심 코드**

```csharp
// Interactor.cs — 화면 중앙에서 레이캐스트로 상호작용 대상 조사
void TryInteract() {
  Ray ray = cam.ViewportPointToRay(center);
  if (Physics.Raycast(ray, out var hit, 2.5f))
    hit.collider.GetComponent<IInteractable>()?.Interact();
}
```

```csharp
// PlayerState.cs — 상태를 ScriptableObject로 분리, 씬 간 공유
[CreateAssetMenu]
public class PlayerState : ScriptableObject {
  public int sanity;
  public bool hasKey;
}
```

**트러블슈팅**
- **카메라 전환이 뚝뚝 끊겼다** — 이벤트마다 카메라를 수동으로 옮기니 전환이 부자연스러웠다. → Cinemachine 가상 카메라 우선순위로 블렌딩해 부드럽게 시점이 넘어가도록 했다.
- **무엇을 상호작용할지 안 보였다** — 플레이어가 조사 가능한 오브젝트를 인지 못 해 답답해했다. → 레이캐스트가 IInteractable에 닿으면 미세한 하이라이트/커서 변화를 줘 단서를 제공했다.

**성과 지표:** 로컬라이즈 3개국어 · Cinemachine 연출 · ScriptableObject 상태 구조 · 팀 3인

**회고 (KPT)**
- Keep: 연출을 Cinemachine으로 위임 / 상태를 데이터로 분리 / 다국어 대응
- Problem: 퍼즐 분량 부족 / 사운드 디자인 미흡
- Try: 퍼즐·엔딩 시퀀스 추가 / 세이브 시스템

**배운 점**
공포는 '놀래키기'가 아니라 '플레이어가 스스로 긴장하게 만드는 설계'라는 걸 배웠다. 연출 도구(Cinemachine)를 잘 위임하는 것도 실력이었다.

**기술 스택:** Unity 2022.3 · C# · URP · Cinemachine · DOTween · Localization

---

## 🎮 TSEROF

> 3D PLATFORMER · UNITY (TEAM)

**TL;DR**

| | |
|---|---|
| 무엇을 | 잃어버린 아이템을 찾아 스테이지를 클리어하는 3D 플랫포머 |
| 왜 | 이동 조작감과 스테이지 진행감을 동시에 살리고 싶었음 |
| 결과 | 팀 협업으로 스테이지 선택·잠금 해제·이어하기 구조 완성 |
| 내 역할 | 게임 프로그래밍 — 플레이어 제어·스테이지 시스템 |

**메타**
- **팀:** 팀 프로젝트 (인원 [확인필요])
- **역할:** 플레이어 제어 · 스테이지 시스템
- **스택:** Unity 2022.3 · C#
- **비고:** 비공개 저장소 (팀원 소유) · 링크는 접근 제한될 수 있음
- **저장소:** https://github.com/KimEoJin24/TSEROF

**문제**
3D 플랫포머에서 스테이지 진행감과 탐색 재미를 동시에 살리려면 이동 조작과 스테이지 구조가 잘 맞물려야 하고, 저장·이어하기까지 고려해야 한다.

**가설**
"정확한 2단 점프 컨트롤 + 스테이지 잠금/저장을 갖추고, 씬을 분리해 작업을 나누면 — 조작감·진행감을 살리면서 팀 머지 충돌도 줄일 수 있다."

**아키텍처**
- **Input** — Player Controller: 이동·2단 점프
- **Flow** — Stage Manager: 선택·잠금 해제·전환
- **Save** — Progress Save: 클리어·이어하기
- **Git** — Team Workflow: 씬 분리로 머지 충돌 최소화

**기술 의사결정**

| 영역 | 선택 | 이유 / 대안 |
|------|------|-------------|
| 점프 | 코요테 타임 | 조작감·관용도↑ ↔ 엄격 판정(좌절↑) |
| 스테이지 | 잠금 해제식 | 진행감 부여 ↔ 전면 개방(동기↓) |
| 저장 | 직렬화 저장 | 구조적 진행 저장·디버그 ↔ PlayerPrefs(구조 빈약) |
| 협업 | 씬 분리 | 머지 충돌 최소화 ↔ 단일 씬(충돌↑) |

**핵심 코드**

```csharp
// PlayerController.cs — 코요테 타임을 둔 2단 점프
void Jump() {
  bool canGround = grounded || coyote > 0;
  if (canGround) { Leap(); jumps = 1; }
  else if (jumps > 0) { Leap(); jumps--; } // 2단
}
```

```csharp
// StageManager.cs — 클리어 시 다음 스테이지 잠금 해제 + 저장
public void Clear(int stage) {
  progress.unlocked = Mathf.Max(progress.unlocked, stage + 1);
  Save.Write(progress); // 이어하기
}
```

**트러블슈팅**
- **같은 씬을 여럿이 건드려 머지 충돌이 잦았다** — Unity .unity 씬 파일은 머지가 어려워 협업 시 충돌이 빈번했다. → 스테이지를 씬/프리팹 단위로 분리하고 담당을 나눠 한 파일을 동시에 만지지 않는 작업 규칙을 세웠다.
- **점프가 '떨어지는 순간' 안 먹혔다** — 발판 끝에서 점프하면 무시되는 경우가 있어 답답했다. → 착지 직후 짧은 코요테 타임을 둬 막 떨어진 순간에도 점프를 허용해 조작감을 높였다.

**성과 지표:** 팀 협업 · 다단계 스테이지 · 이어하기 저장 · Unity 2022.3

**회고 (KPT)**
- Keep: 코요테 타임으로 조작감 개선 / 씬 분리로 협업 충돌 최소화
- Problem: 스테이지 분량 부족 / 밸런싱 시간 부족
- Try: 보스 패턴 추가 / 리플레이·타임어택

**배운 점**
팀 협업에서 '기술'만큼 '충돌 안 나게 일 나누는 구조'가 중요하다는 걸 배웠다. 조작감은 코요테 타임 한 줄의 디테일에서 갈렸다.

**기술 스택:** Unity 2022.3 · C#

---

> ℹ️ 신규 3개 프로젝트(Ajou Campus Foodmap · The Other Side(VR) · INTO MONSTER POINT(AR))는 아직 상세(원페이지) 데이터가 없어 카드/링크 수준만 있습니다. 필요하면 같은 형식으로 상세를 채워드릴 수 있어요.
