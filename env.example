# Frontend
# Copy this value into .env.local when running Next.js locally.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Backend variables live in backend/.env.
# Copy backend/.env.example to backend/.env before running FastAPI.
