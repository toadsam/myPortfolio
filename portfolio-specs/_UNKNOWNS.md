# 확인 필요 목록 — 형님만 답할 수 있는 것들

> 작성 2026-07-31. **저장소 코드로는 확정할 수 없어서 비워둔 것들만** 모았습니다.
> 이미 검증이 끝난 사실은 `_AUDIT.md`, 수정 이력은 `_STATUS.md` §6 을 보세요.
>
> **읽는 법**
>
> - 🔴 **A급** = 답을 모르면 그 페이지를 **못 씁니다** (지금 서술이 근거 없음)
> - 🟡 **B급** = 페이지는 쓸 수 있지만 **면접에서 되물으면 막힙니다**
> - ⚪ **C급** = 캡처/촬영하면 자연히 채워지는 것
>
> **답을 못 찾겠으면 지어내지 말고** 해당 카드를 "재보지 못한 것 / 확인 필요"로 노출하세요.
> 그게 이 포트폴리오 전체의 규칙입니다 (`00-OVERVIEW.md` §3 규칙 2).

---

## 🔴 A급 — 이걸 모르면 페이지를 못 씁니다

### A-1. 07 수어지교 P02·P06 — 프레임 데이터가 없습니다

현재 두 페이지가 **"내 API가 손동작 프레임 데이터를 내려줬다"** 를 전제로 서 있습니다.
그런데 저장소에는 그런 게 없습니다:

```
backend/src/main/resources/static/choicemp4/q001.mp4 … q010.mp4   ← 완성된 MP4
백엔드 응답 필드: questionText · choices · correctChoiceId · videoUrl
                 difficulty_level · attempt_count · correct_count · incorrectQuestions
```

`keyframe` / `pose` / `frame` / `interpolation` 관련 코드·데이터 **0건**.
게다가 3D 아바타는 **팀원(4인 중 1인) 저장소**라 형님 작업으로 내세우기 어렵습니다.

**답해주실 것**

- ☐ **P06 「손이 뚝뚝 끊겼다」 — 실제로 겪은 백엔드 트러블이 뭐였나요?**
  후보: 오답 집계가 안 맞았다 / 같은 문제만 반복 출제됐다 / 정답률 통계가 어긋났다 /
  영상이 늦게 떴다 / 그 외( )
  → **기억이 없으면 P06을 트러블슈팅에서 빼고 「오답노트 설계」 페이지로 바꾸는 게 정직합니다.**
- ☐ **P02 「동작을 데이터로」 — 「단어 하나가 화면에 뜨기까지」로 바꿔도 될까요?**
  (Firestore 문항 → 보기 4개 → mp4 URL 해결 → 채점 → 오답 누적. **전부 코드로 증명됨**)

> ⚠️ SVG 손을 **화면 연출**로 쓰는 건 문제없습니다. 그건 포트폴리오의 삽화지 제품에 대한 주장이 아닙니다.
> 문제는 **"내 API가 프레임을 내려줬다"** 는 주장 하나뿐입니다.

---

### A-2. 05 ACLUB 트러블 2건 — 코드로 확인 못 했습니다

`aClub2026/FE` 를 받아봤지만, 아래 두 주장은 **코드에 흔적이 남는 종류가 아닙니다.**

| `data.ts` 현재 서술                            | 확인 필요                                                              |
| ---------------------------------------------- | ---------------------------------------------------------------------- |
| "여러 화면이 같은 데이터를 제각각 불렀다"      | ☐ 실제로 있었나요? 해결은 뭐였나요 (공통 훅 / 캐시 / 상태 끌어올리기)? |
| "권한 체크를 화면마다 넣다 보니 누락이 생겼다" | ☐ 실제로 누락이 났나요? 보호 라우트로 바꾼 게 맞나요?                  |

**한편 배포 사이트(`src/portfolio.js`)에는 더 구체적인 게 있습니다** — 이쪽이 더 좋은 재료입니다:

- **GA4 / Google Search Console 기반 개선 루프** (감이 아니라 지표로 우선순위 결정)
- **401 → HttpOnly Refresh 쿠키로 재발급 → 원요청 자동 재시도**
- Everytime·카카오톡 홍보 유입을 상세 상단 고정 CTA로 연결
- ☐ **05 방을 위 3개 중심으로 다시 잡을까요?** (지표 수치는 절대 지어내지 않고 "화면 캡처로만" 제시)

---

### A-3. 06 아주총학 트러블 2건 — 절반만 확인됨

| `data.ts` 현재 서술             | 상태                                                                                                                    |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| "새로고침하면 404가 떴다"       | ✅ **검증됨** — `nginx.conf` 의 `try_files $uri /index.html;` + `error_page 404 /index.html;` 가 그 해결의 증거         |
| "로컬은 되는데 서버에선 달랐다" | 🟡 **정황만 있음** — `Dockerfile` 이 멀티스테이지 + `ARG REACT_APP_GOOGLE_CLIENT_ID` 빌드타임 주입 구조라 개연성은 높음 |

- ☐ **두 번째가 구체적으로 뭐였나요?** (환경변수 미주입 / API 주소 / CORS / 빌드 산출물 경로 등)
  → 모르면 **"로컬은 되는데 서버에선 달랐다"** 라는 뭉뚱그린 표현 대신 그 페이지를
  **「Docker + Nginx로 직접 배포한 이야기」** 로 바꾸는 게 낫습니다. 이건 전부 코드로 증명됩니다.

---

## 🟡 B급 — 페이지는 쓰지만 되물으면 막힙니다

### B-1. 02 MyStock — 평단 버그를 실제로 겪었나

- 코드상 시나리오는 **성립합니다**: 전량 매도 시 `holdingRepository.delete(holding)` → 재매수 시 신규 `Holding` 생성
- 하지만 커밋이 1개뿐이라 **"그 버그를 겪고 고쳤다"는 증거는 없습니다**
- ☐ 실제로 이 증상을 본 적 있나요? 없으면 P04를 **「전량 매도 후 재매수를 어떻게 처리할지 정한 설계 결정」** 으로
  (트러블슈팅 → 의사결정으로) 바꾸는 게 정직합니다. `−99.9%` 수치는 이미 삭제했습니다.

### B-2. 09 DarkLab — README만 보고 판단했습니다

저장소가 3.2GB라 클론하지 않고 **README + 파일 목록만** 확인했습니다.
README에 Cinemachine · 레이캐스트 상호작용 · ScriptableObject가 **명시돼 있어** `data.ts` 의 트러블 2건은
✅ 로 판정했지만, **실제 스크립트를 읽은 건 아닙니다.**

- ☐ P04(손전등이 커서를 안 따라오는 3초) · P06(스크롤 1.5초 잠김) 연출이 **실제 게임의 어떤 문제**에 대응하나요?
  → 지금 문서는 연출만 있고 대응하는 개발 실체가 흐릿합니다.

### B-3. 10 TSEROF — 팀원별 담당 범위

- ✅ 확정: 2023.11–2024.02 · 5인 · Steam 출시 · 스테이지 3종 + StageSelect
- ☐ **내가 한 것 / 팀원이 한 것** 경계 (P08이 이 표 위에 서 있습니다)
- ☐ `FileDataHandler` 의 `_useEncryption` 기본값이 `false` 인데 **실제 빌드에서 켜져 있었나요?**
  (켜져 있었다면 XOR 세이브가 실제 기능, 아니면 "구현했지만 끄고 출시"로 써야 정확합니다)
- ☐ P06 「같은 씬 파일 머지 충돌」 — 실제로 **누군가의 작업이 사라진 적**이 있었나요?
  (현재 명장면 서술이 그걸 전제로 함)

### B-4. 04 득근득근 P06 「출석이 두 번 찍혔다」

멱등성 처리 페이지인데 **해당 코드를 확인하지 못했습니다.**

- ☐ 실제로 중복 지급이 났었나요? 해결이 뭐였나요 (유니크 제약 / 요청 키 / 트랜잭션)?

### B-5. 03 FestFlow 재연결 임계값

P10 PROBLEM에 **"재연결 임계값을 근거 없이 감으로 정했다"** 고 적혀 있습니다.

- ☐ 이 문장이 사실인가요? (사실이면 그대로 두는 게 오히려 좋은 자기인식입니다)

---

## ⚪ C급 — 캡처하면 채워집니다

- ☐ **10 TSEROF 시연 영상 길이** — 문서 3곳이 `[확인필요]` (`00-OVERVIEW.md` §5-1 총계도 이것만 비어 있음)
- ☐ **미디어 69장 + 영상 10편** — `00-OVERVIEW.md` §5-3 의 작업 묶음 순서대로.
  시간 없으면 묶음 **C(Unity 에디터 4장) · E(터미널 3장) · F(콘솔 1장) = 8장**부터 (전부 최상 우선도)
- ☐ **07 파란학기 진행 기간** — `data.ts` meta 가 아직 `[확인필요]`

---

## 📌 답하실 때

이 파일에 **직접 답을 적어주셔도 됩니다.** 각 ☐ 옆에 한 줄이면 충분합니다.
답이 들어오면 해당 문서와 `data.ts` 를 같이 고치겠습니다.

**"기억이 안 난다"도 유효한 답입니다.** 그 경우 해당 페이지를
트러블슈팅 → 설계 결정 / 구현 소개로 바꾸면 됩니다. 지어내는 것보다 항상 낫습니다.
